@model IEnumerable<AgendaMedica.Models.Sector>

<div class="text-end">
    <a asp-action="create" class="btn btn-primary">Crear Sector</a>

</div>
<table class="table table-bordered">
    <thead class="bg-dark text-white">
        <tr>
            <th>ID</th>
            <th>Sector</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.IdSector</td>
                <td>@item.Sector1</td>
                <td>
                        <a asp-action="Edit" asp-route-id="@item.IdSector " 
                        class="btn btn-warning"><i class="bi bi-pencil-square"> </i>Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.IdSector "
                        class="btn btn-danger"><i class="bi bi-trash"></i> Eliminar</a>
                </td>
            </tr>
        }
        </table>